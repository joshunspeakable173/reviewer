# Work Artifacts

The pipeline writes parsed text, run-specific prompts, reviewer JSON, logs, and editor bundles under `work/<paper_id>/`.

These artifacts can contain copyrighted paper text and reviewer outputs, so directory contents are ignored by Git. Keep only this README tracked.
