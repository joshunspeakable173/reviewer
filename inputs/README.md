# Inputs

Put source paper PDFs in this directory before running the reviewer pipeline.

The contents of this directory are ignored by Git so private papers are not committed. Keep only this README tracked.
