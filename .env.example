# Copy to .env or set these as user-level environment variables when needed.
# Do not commit real credentials.

# Required only if your Codex/OpenAI setup uses an API key instead of CLI login.
OPENAI_API_KEY=

