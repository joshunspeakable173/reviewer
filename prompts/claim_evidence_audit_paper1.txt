Use the custom agent named claim_evidence_auditor.

Use the repo guidance and the paper-reviewer skill.

Audit:
`work/paper1/parsed/`

Return one JSON object that strictly conforms to:
`schemas/reviewer_output.schema.json`

For this run:
- reviewer = "claim_evidence_auditor"
- paper_id = "paper1"

Focus on:
- whether major textual claims appear supported by the displayed evidence
- overstatement
- weak support
- missing support
- cannot_verify when the available artifacts do not allow a check
