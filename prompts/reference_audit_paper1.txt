Use the custom agent named reference_auditor.

Use the repo guidance and the paper-reviewer skill.

Audit:
`work/paper1/parsed/`

Return one JSON object that strictly conforms to:
`schemas/reviewer_output.schema.json`

For this run:
- reviewer = "reference_auditor"
- paper_id = "paper1"

Focus on:
- in-text citations
- reference-list entries
- consistency between them
- missing, duplicate, outdated, or cannot_verify cases
