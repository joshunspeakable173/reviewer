# Outputs

Final reports are written under `outputs/<paper_id>/report.md`.

Reports can contain paper-specific quotations and analysis, so directory contents are ignored by Git. Keep only this README tracked.
